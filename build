# -*- mode: python ; coding: utf-8 -*-
from PyInstaller.utils.hooks import collect_all

# กำหนดไฟล์ข้อมูลที่ต้องรวม
datas = [
    ('user_config.json', '.'),
    ('booking_elements', 'booking_elements'), # รวมทั้งโฟลเดอร์ booking_elements
    ('branch', 'branch'), # รวมทั้งโฟลเดอร์ branch
]

# กำหนด hidden imports ที่ PyInstaller อาจหาไม่เจออัตโนมัติ
hiddenimports = [
    'tkinter', 'tkinter.ttk', 'gspread', 'google.oauth2.service_account',
    'schedule', 'requests',
    'live_mode', # gui_app.py จะ import live_mode โดยตรง
    'trial_mode', # gui_app.py จะ import trial_mode โดยตรง
    'utils',     # gui_app.py จะ import utils โดยตรง
]

# รวบรวม Playwright dependencies ทั้งหมด
tmp_ret = collect_all('playwright')
datas += tmp_ret[0]
binaries = tmp_ret[1]
hiddenimports += tmp_ret[2]

a = Analysis(
    ['gui_app.py'], # <--- **แก้ไขตรงนี้** จาก 'main.py' เป็น 'gui_app.py'
    pathex=[],
    binaries=binaries,
    datas=datas,
    hiddenimports=hiddenimports,
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=['PyQt5', 'PySide6'],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='PopmartthBookingBot', # ชื่อไฟล์ executable
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=False, # ตั้งค่าเป็น False สำหรับ GUI application
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='PopmartthBookingBot',
)
